import React from "react";
import {DocBase} from "./common";

export default function Page() {
    return <DocBase>
        <h1 className="font-bold text-2xl mb-4">Getting started with GreenJS</h1>
        <p>To start using GreenJS, run <code>npx create-greenjs-app</code></p>
        <p>This will create a folder named <i>greenjs-example</i> that will contain the standard GreenJS directory structure:</p>

    </DocBase>
}